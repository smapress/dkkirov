.group-input-fields-content{"data-callback-render" => 'custom_field_advance'}
  %input{class: "form-control input-value #{'is_translate' if field.options[:translate].to_s.to_bool} #{"required" if field.options[:required].to_s.to_bool}",
         name: "#{field_name}[#{field.slug}][values][]",
         type: 'text'}
